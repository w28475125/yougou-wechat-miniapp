.search-input{
	height:90rpx;
	padding: 10rpx;
	background-color: var(--themeColor);
}
.search-input navigator{
	height:100%;
	display: flex;
	justify-content: center;
	align-items: center;
	border-radius: 10rpx;
	background-color: #fff;
	color: var(--font66Color);
}